# Raw email files (may contain personal data)
inputs/emails/*.eml

# Analysis data (generated per-user)
data/tracking-domains.json
data/sender-blocklist.json
data/processed-emails.json
outputs/analyses/*.md

# Keep sanitised samples
!inputs/emails/sanitised/
